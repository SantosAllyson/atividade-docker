name: Testar build Docker

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Baixar código
        uses: actions/checkout@v4

      - name: 🐳 Testar Dockerfile
        run: docker build -t meu-site-docker .